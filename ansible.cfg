[defaults]
remote_user = ansible
inventory = ./inventory
roles_path = ./roles
collections_path = ./collections:/usr/share/ansible/collections

[privilege_escalation]
become = true
become_user = root
become_method = sudo
become_ask_pass = false
